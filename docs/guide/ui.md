---
title: 终端 UI
---

## 交互界面

`crystal chat` 会打开终端 UI，用于：

- 展示流式输出
- 展示工具调用与工具结果
- 快速演示与调试

## 快捷键

- `?`：显示/隐藏帮助
- `Ctrl+X`：中断当前 loop
- `Ctrl+L`：清屏
- `Ctrl+C` / `Esc`：退出

## 演示建议

- 在输入框下方的状态行确认当前 `provider/model` 与 `sessionId`
- 先用 `/search` 或 `/read` 触发一次工具调用，让观众看到“模型不是只会说，会在仓库里动手”

